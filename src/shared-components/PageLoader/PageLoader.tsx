export function PageLoader() {
  return (
    <div
      style={{
        minHeight: "100vh",
        display: "grid",
        placeItems: "center",
        color: "#175cd3",
        fontWeight: 700
      }}
    >
      Loading...
    </div>
  );
}
